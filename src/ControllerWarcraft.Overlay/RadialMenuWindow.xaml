<Window x:Class="ControllerWarcraft.Overlay.RadialMenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        Focusable="False"
        IsHitTestVisible="False"
        ResizeMode="NoResize"
        Width="380" Height="380">

    <!-- Canvas su cui il code-behind disegna i settori (wedge) e le etichette.
         La finestra è click-through e non-attivabile: è solo un indicatore visivo. -->
    <Canvas x:Name="RadialCanvas" Width="380" Height="380" />
</Window>
